{{- if .Values.serviceMonitor.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "remla-app.fullname" . }}-grafana-dashboard
  labels:
    {{- include "remla-app.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  app-service-dashboard.json: |-
{{ .Files.Get "dashboards/app-service-dashboard.json" | nindent 4 }}
{{- end }}